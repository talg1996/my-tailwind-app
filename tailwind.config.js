/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Include all your React components
  ],
  darkMode: "class", // Enable class-based dark mode
  theme: {
    extend: {
      // Add custom themes, colors, fonts, etc. here if needed
    },
  },
  plugins: [],
};
